const kea = require('./data/kea.txt')
const kiwi = require('./data/kiwi.txt')
const nikau = require('./data/nikau.txt')
const pohutukawa = require('./data/pohutukawa.txt')

const options = [
  kea, kiwi, nikau, pohutukawa
]

// function binds (input) {
//   for (let i = 0; i < options.length; i++) {
//     if (input = i) {
//       show(options[i])
//     }
//   }
// }

module.exports = options
